# bun + elysia + vike-server

install deps
```shell
bun install
```

run dev
```shell
bun run dev
```

run prod like
```shell
bun run prod
```

run build only
```shell
bun run build
```

start prod server only
```shell
bun run start
```
